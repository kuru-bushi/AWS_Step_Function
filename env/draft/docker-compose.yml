version: '3'

services:
  cmake:
    container_name: AML2023_step_functions
    build:
      context: .
      dockerfile: Dockerfile

    tty: true
    command: /bin/bash

    volumes:
      - ../../:/home
